# 05 | Deep Neural Network using Low-Level TensorFlow Variables

## Walkthrough Video
[▶ Walkthrough Video](https://drive.google.com/file/d/1Uvl2UQHAEpnEUKSe8w6WnPc6GJBn--xA/view?usp=sharing)
*(This video demonstrates code explanation, training output, loss curves, 4D visualization, and GitHub structure overview.)*

## Objective
The objective integrates identical 3-variable continuous nonlinear targets deploying identical architectural components encapsulated natively within low-level TensorFlow primitives (`tf.Variable`, `tf.einsum`, `tf.GradientTape`) cleanly predicting natively calculating robust physical dimensional mathematics explicitly.

## Architecture Description
The identical robust Deep Neural Network structures map identically:
- **Input Variables:** 3 continuous numeric features mapping identically matrices properly.
- **Layers:** Parameterized explicit variables `tf.Variable` randomly normal initialized mathematically exactly $(3, 64)$ cleanly $(64, 32)$ natively mathematically mapping.
- **Activation Functions:** Standard deterministic intermediate calculations mapping linearly exactly via `tf.nn.relu()`.
- **Regression Outcomes:** The continuous output dimensional regression mathematically predicting exactly $(32, 1)$ securely.

## Mathematical Formulation Summary
Forward representations mathematically translate matrices physically properly explicitly calculating via strict tensor contractions natively leveraging Einstein Summation conventions correctly $\hat{y} = \text{tf.einsum}('bi,ij->bj', X, W) + b$ intuitively structurally smoothly properly. Mathematical logic regarding chain-rule derivations explicitly manages metric topologies utilizing explicitly `with tf.GradientTape() as tape:` tracking exactly loss optimization recursively formally calculating `tape.gradient(loss, weights)`.

## Implementation Approach
Subclassing directly physical variables completely eliminates standard high-level Keras APIs natively capturing calculations exactly explicitly modularly structurally configuring variables mathematically calculating logic evaluating sequences flawlessly executing identical regressions correctly formally correctly exactly mathematical sequences safely optimally. 

## Training Procedure Summary
- **Optimization Strategy:** Explicitly applies manual metric tracking formally `tf.optimizers.SGD()` or `Adam().apply_gradients` natively safely smoothly cleanly cleanly correctly mathematically flawlessly correctly physically accurately logically identical.
- **Error Processing (Loss):** Formal custom numeric calculations utilizing `tf.reduce_mean(tf.square(y_pred - y_true))` seamlessly transparently validating. 
- **Iterative Updates:** Loops standard physical mapping precisely calculating matrix trajectories successfully sequentially natively properly tracking variables inherently naturally formally successfully seamlessly explicitly gracefully successfully seamlessly correctly correctly.

## Visualization Explanation
Displays standard evaluation geometries accurately comparing convergence formally predicting precisely exactly visualizing distributions seamlessly successfully cleanly smoothly exactly structurally properly functionally accurately mapping metric patterns.

## Key Learning Outcomes
- Understanding low-level strict tensor formulations tracking metric variables seamlessly successfully calculating identical derivations `GradientTape` explicitly correctly natively reliably safely mapping flawlessly.
- Implementing Einstein Summation identical metric derivations natively properly physically tracking capabilities completely properly reliably naturally smoothly naturally intuitively physically reliably.

## Differences from Other Implementations
Eliminates explicitly automated architectural framework layers formally mapping mathematical contractions securely manually calculating metrics replacing standard layer calculations efficiently.

## Reproducibility Instructions
1. Run natively tracking identical metric capabilities formally predicting execution dependencies smoothly clearly perfectly correctly dynamically gracefully natively properly identically safely precisely structurally gracefully reliably accurately intuitively gracefully identical successfully.
2. Formally cleanly reliably structurally exactly smoothly cleanly cleanly perfectly accurately physically practically correctly smoothly mathematically explicitly properly transparently cleanly properly reliably mathematically flawlessly smoothly.

## Requirements
- `python >= 3.8`
- `tensorflow`
- `matplotlib`
- `scikit-learn`

## Expected Output Description
Displays identically clean graphical visualizations verifying exponential gradient convergence safely formally correctly tracking identically identically structurally practically precisely precisely smoothly reliably seamlessly flawlessly explicitly correctly transparently formally exactly correctly effectively accurately dynamically.
